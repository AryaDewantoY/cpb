export type ProjectType = {
  imgSrc: string
  name: string
  description: string
}

export type defaultText= {
    safety: string;
    value: string;
    transpasi: string;
    expierience: string;
    // rebuild: string;
}